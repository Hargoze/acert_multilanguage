# Migration `20201008151702`

This migration has been generated by Hargoze at 10/8/2020, 5:17:02 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Section" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "page" TEXT NOT NULL DEFAULT 'product',
    "title" TEXT NOT NULL,
    "content" TEXT NOT NULL,
    "color" TEXT NOT NULL DEFAULT 'black',
    "style" TEXT,
    "ImageId" INTEGER,
    "direction" TEXT DEFAULT 'right',
    "link" TEXT,
    "href" TEXT,
    "link_color" TEXT,
    "background_color" TEXT,
    "background_image" TEXT,
    "bgimg_props" TEXT,
    "ps" TEXT,
    "ps_position" BOOLEAN DEFAULT true,
    "ps_color" TEXT,
    "list" TEXT,

    FOREIGN KEY ("ImageId") REFERENCES "Section_Image"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Section" ("id", "title", "content", "color", "style", "link", "href", "link_color", "ps", "ps_color", "list", "background_color", "background_image", "bgimg_props", "ps_position", "page", "direction") SELECT "id", "title", "content", "color", "style", "link", "href", "link_color", "ps", "ps_color", "list", "background_color", "background_image", "bgimg_props", "ps_position", "page", "direction" FROM "Section";
DROP TABLE "Section";
ALTER TABLE "new_Section" RENAME TO "Section";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

PRAGMA foreign_keys=off;
DROP TABLE "_SectionToSection_Image";
PRAGMA foreign_keys=on
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201008145619-change-images..20201008151702
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -89,9 +89,9 @@
   name String
   alt String @default("alt")
   width Int?
   length Int?
-  section  Section[] @relation(references: [id])
+  section  Section[]
 }
 model Section {
   id    Int     @default(autoincrement()) @id
@@ -100,9 +100,10 @@
   title String
   content String
   color String @default("black")
   style String?
-  image Section_Image[] @relation(references: [id])
+  image Section_Image? @relation(fields: [ImageId], references: [id])
+  ImageId Int?
   direction String? @default("right")
   link String?
   href String?
   link_color String?
```


