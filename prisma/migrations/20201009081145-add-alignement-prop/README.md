# Migration `20201009081145-add-alignement-prop`

This migration has been generated by Hargoze at 10/9/2020, 10:11:45 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Section" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "page" TEXT NOT NULL DEFAULT 'product',
    "title" TEXT NOT NULL,
    "content" TEXT NOT NULL,
    "color" TEXT NOT NULL DEFAULT 'black',
    "style" TEXT,
    "button_alignement" TEXT NOT NULL DEFAULT 'start',
    "ImageId" INTEGER,
    "direction" TEXT DEFAULT 'right',
    "link" TEXT,
    "href" TEXT,
    "link_color" TEXT,
    "background_color" TEXT,
    "background_image" TEXT,
    "bgimg_props" TEXT,
    "ps" TEXT,
    "ps_position" BOOLEAN DEFAULT true,
    "ps_color" TEXT,
    "list" TEXT,

    FOREIGN KEY ("ImageId") REFERENCES "Section_Image"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Section" ("id", "title", "content", "color", "style", "link", "href", "link_color", "ps", "ps_color", "list", "background_color", "background_image", "bgimg_props", "ps_position", "page", "direction", "ImageId") SELECT "id", "title", "content", "color", "style", "link", "href", "link_color", "ps", "ps_color", "list", "background_color", "background_image", "bgimg_props", "ps_position", "page", "direction", "ImageId" FROM "Section";
DROP TABLE "Section";
ALTER TABLE "new_Section" RENAME TO "Section";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201009071819-change-img-size..20201009081145-add-alignement-prop
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -94,14 +94,15 @@
 }
 model Section {
   id    Int     @default(autoincrement()) @id
-  buttons Button[] @relation(references: [id])
   page String @default("product")
   title String
   content String
   color String @default("black")
   style String?
+  button_alignement String @default("start")
+  buttons Button[] @relation(references: [id])
   image Section_Image? @relation(fields: [ImageId], references: [id])
   ImageId Int?
   direction String? @default("right")
   link String?
```


