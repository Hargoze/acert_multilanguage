# Migration `20201008145619-change-images`

This migration has been generated by Hargoze at 10/8/2020, 4:56:19 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Section_Image" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "alt" TEXT NOT NULL DEFAULT 'alt',
    "width" INTEGER,
    "length" INTEGER
)

CREATE TABLE "_SectionToSection_Image" (
    "A" INTEGER NOT NULL,
    "B" INTEGER NOT NULL,

    FOREIGN KEY ("A") REFERENCES "Section"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("B") REFERENCES "Section_Image"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Section" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "page" TEXT NOT NULL DEFAULT 'product',
    "title" TEXT NOT NULL,
    "content" TEXT NOT NULL,
    "color" TEXT NOT NULL DEFAULT 'black',
    "style" TEXT,
    "direction" TEXT DEFAULT 'right',
    "link" TEXT,
    "href" TEXT,
    "link_color" TEXT,
    "background_color" TEXT,
    "background_image" TEXT,
    "bgimg_props" TEXT,
    "ps" TEXT,
    "ps_position" BOOLEAN DEFAULT true,
    "ps_color" TEXT,
    "list" TEXT
);
INSERT INTO "new_Section" ("id", "title", "content", "color", "style", "link", "href", "link_color", "ps", "ps_color", "list", "background_color", "background_image", "bgimg_props", "ps_position", "page", "direction") SELECT "id", "title", "content", "color", "style", "link", "href", "link_color", "ps", "ps_color", "list", "background_color", "background_image", "bgimg_props", "ps_position", "page", "direction" FROM "Section";
DROP TABLE "Section";
ALTER TABLE "new_Section" RENAME TO "Section";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

CREATE UNIQUE INDEX "_SectionToSection_Image_AB_unique" ON "_SectionToSection_Image"("A", "B")

CREATE INDEX "_SectionToSection_Image_B_index" ON "_SectionToSection_Image"("B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201008142725..20201008145619-change-images
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -83,17 +83,26 @@
   href String?
   file String?
 }
+model Section_Image {
+  id    Int     @default(autoincrement()) @id
+  name String
+  alt String @default("alt")
+  width Int?
+  length Int?
+  section  Section[] @relation(references: [id])
+}
+
 model Section {
   id    Int     @default(autoincrement()) @id
   buttons Button[] @relation(references: [id])
   page String @default("product")
   title String
   content String
   color String @default("black")
   style String?
-  image String?
+  image Section_Image[] @relation(references: [id])
   direction String? @default("right")
   link String?
   href String?
   link_color String?
```


